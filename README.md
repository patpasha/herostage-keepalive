# herostage-keepalive

Public ping every 5 minutes so the Render free instance for Hero Stage does not sleep.

Target: `https://heromode-spidey.onrender.com/api/`

This lives in a **public** repo because GitHub Actions minutes are unlimited there. The private app repo would exhaust its quota in a few days.

Manual run: Actions → Keep Render awake → Run workflow.
